<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <application>

        <!-- The main service, handles playback, playlists and media buttons -->
        <service
            android:name="com.doublesymmetry.trackplayer.service.MusicService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- Media3 canonical media-button broadcast entrypoint.
             Required for OEMs (Xiaomi/MIUI/HyperOS, FunTouchOS, ColorOS,
             Realme, Honor MagicOS) whose system UI dispatches notification
             and Bluetooth play/pause taps as broadcasts rather than as
             direct MediaSession controller events. Receiver locates this
             app's MediaLibraryService via SERVICE_INTERFACE and forwards
             the KeyEvent via startForegroundService() — landing in
             onStartCommand(), where the existing fall-through handles it. -->
        <receiver
            android:name="androidx.media3.session.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
